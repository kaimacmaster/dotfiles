# User configuration
export EDITOR='nvim'

# Aliases
alias ll='ls -alF'
alias yd='nvm use && yarn dev'
alias yid='nvm use && yarn && yarn dev'
alias nv='nvim'
alias gs='git status'
alias gc='git commit -m'
alias ga='git add'

